# Redpoint.Uefs.Registry

This library provides various utilities for parsing Docker tags and obtaining Docker credentials from the local system.

This library is intended for internal use inside UEFS only.
