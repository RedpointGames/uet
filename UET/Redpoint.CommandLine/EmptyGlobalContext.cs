namespace Redpoint.CommandLine
{
    /// <summary>
    /// An empty global context object that is used so that our implementations can re-use the same logic for both global-context and no-global-context APIs.
    /// </summary>
    internal class EmptyGlobalContext
    {
    }
}
