apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: rkmnodegroups.rkm.redpoint.games
spec:
  group: rkm.redpoint.games
  scope: Cluster
  names:
    plural: rkmnodegroups
    singular: rkmnodegroup
    kind: RkmNodeGroup
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              provisioner:
                type: string
                nullable: true
                description: Specifies the provisioner to use to provision this machine.
    selectableFields: []
    additionalPrinterColumns:
    - jsonPath: .spec.provisioner
      name: "Provisioner"
      type: string
